# 仕様書：可変サニタイズ・ルール・マネジメント構造

## 1. 設計哲学
このアプリは「聴くほどに賢くなる」べきであり、それは単に新たな機能を追加するだけでなく、**「テキストの浄化（サニタイズ）ルールそのものが、ユーザーからのフィードバックを通じて自律的に進化する」**ことを意味する。

単一のハードコーディングされたロジックに依存するのではなく、ルールを保守性の高い多層構造で管理し、開発者（私）およびユーザー（アプリの利用者）が安全に・ルールベースでコントロールできるようにする。

## 2. 多層ルール・マネジメント構造 (3-Tier Rule Management)

ルールの解決と適用は、以下の優先順位に従う。

| 層 | 役割 | 格納場所 | 保守主体 | 優先度・動作 |
| :--- | :--- | :--- | :--- | :--- |
| **L1: 既定値 (ファイル)** | アプリケーションの最低限の品質保証。<br>壊れては困るコアルールの集合。 | `src/config/sanitizeRules.json`<br>(バージョン管理対象) | **開発者（私）**<br>コードベースで保守。 | **ベースライン。**<br>破壊的入力・既知の致命的問題を防ぐ。<br>原則として削除には明示的なレビューが必要。 |
| **L2: 環境変数 (ランタイム)** | デプロイ時・テスト時の一時的な調整。<br>特定のルールを無効化/強化したり、モードを切り替えるためのスイッチ。 | `.env.local` / `.env.production`<br>(Cloudflare Workers secret) | **開発者（私）**<br>デプロイ設定。 | **L1に勝る。**<br>L1のルールを上書き・無効化可能。<br>新しい機能を安全にテストするため。 |
| **L3: 動的ルール (D1/Supabase)** | ユーザーからのフィードバックを元に、AIキュレーターが「提案・生成」した新ルール。<br>アプリが「リアルタイムで賢くなった」証拠。 | `guide_sanitize_rules` テーブル (D1) <br>または Supabase | **ユーザーからのフィードバック**<br>+ **AIキュレーター** | **L1, L2に勝る（最高）。**<br>ユーザーの指摘を受けて提案された新たなパターンを、安全性を検証した上で適用。 |

## 3. 保守経路とワークフロー

### 3.1 開発者による保守 (L1, L2)
- 開発者は `src/config/sanitizeRules.json` を直接編集し、Git でコミットすることで、基盤となるルールを更新できる。
- 環境変数経由で、一時的なルール上書きやテストモードの指定ができる。
- これにより、「コードの変更なく、設定変更でアプリの振る舞いを安全に調整できる」ことを保証する。

### 3.2 ユーザーからのフィードバックによる自律修復 (L3)
1. **検出**: ユーザーが、「解説に挨拶が二つある」「マークダウンが崩れている」などのフィードバックを入力する。
2. **分析**: AIキュレーター（`/api/feedback`）が、対象作品の既存JSONをD1から読み込み、問題の箇所を特定する。
3. **提案**: AIが「このようなテキストを除去すべきだ」という新しいサニタイズ・ルール（パターン＋アクション）を提案する。
4. **格納**: 提案されたルールは、L3（動的ルール格納庫）に格納され、以降の `/api/chat` / `/api/sanitize` 呼び出し時、L3ルールとして優先適用される。
5. **学習**: 以後、同様のコンテンツが含まれる作品に対しては、自動的にこのルールが適用されるようになり、アプリはそのユーザーおよび以後のユーザーに対して「賢くなった」状態で振る舞う。

## 4. 実装ガイドライン
- **ルール・マネージャー (`src/lib/ruleManager.ts`)**:<br>L1 $\rightarrow$ L2 $\rightarrow$ L3 の順にルールを取得・統合し、最終的なルールセットをサニタイザーに提供する。
- **汎用サニタイザー (`src/lib/sanitizer.ts`)**:<br>「ハードコーディングされた関数」は廃止し、ルール・マネージャー経由で渡されたルールを実行する汎用関数とする。
- **安全性**:<br>L1の削除・無効化には明確なレビューを要する。L3は、特定のコンテンツにのみ過剰に適用されるリスクがあるため、汎化（generalize）のチェックを推奨する。

## 5. 成功基準
- 開発者がL1ファイルを編集するだけで、基本的なサニタイズ動作を安全に制御できる。
- ユーザーからのフィードバックが、L3ルールとして蓄積され、以後同種の不具合が自動的に減少する。
- アプリが「聴くほどに賢くなる」ことを、ルールの進化として具体的に観測できる。

---
*本仕様は、ArtFreeGuide プロジェクトの「聴くほどに賢くなる」哲学を実装するための基盤定義である。*
