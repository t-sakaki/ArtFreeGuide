# Bug Report: 物理的ノイズの発生と音声合成の停止問題

## 現状の深刻なリスク
- **物理的危険性**: 再生ボタン押下時に、スピーカーが激しく振動するほどの低音ノイズが発生している。これはハードウェア（スピーカー）の破損およびユーザーの聴覚に深刻な影響を与えるリスクがある。
- **機能的バグ**: `SpeechSynthesis` (Web Speech API) が1行目で停止し、それ以降の行に遷移しない問題が継続している。

## 切り分け戦略の正当性
原因が以下のいずれにあるのかを特定する必要がある：
1. `SpeechSynthesis` API 自体の不具合またはブラウザの制限。
2. `Web Audio API` (環境音) によるノイズ生成およびリソース競合。
3. 行遷移を制御するロジック（`onEnd` コールバックの不発など）の不備。

実音声出力を完全に排除し、コンソールログによる擬似的な時間経過を導入することで、**「物理的リスクをゼロにした状態で、ロジックのみを安全に検証する」** ことが可能となる。これにより、ロジックが正しければハイライトが最後まで遷移することを証明でき、問題が「音声出力層」にあることを切り分けることができる。
