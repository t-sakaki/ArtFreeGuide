# Implementation Plan: 読み上げエリアの操作性向上

## 1. Summary
ユーザー体験の向上を目的とし、音声ガイド再生中のテキストエリアにおいて「ドラッグによるスクロール（Grab to Scroll）」機能を導入し、操作の起点となる再生ボタンをUIの最左端に再配置します。

## 2. Scope
- **修正ファイル**: `/home/taira/ArtFreeGuide/src/app/page.tsx`
- **影響範囲**:
    - **UIレイアウト**: 下部コントローラーパネル（再生ボタンの配置順序）。
    - **インタラクション**: 読み上げセグメント表示エリア（マウス/タッチイベントによるスクロール制御）。

## 3. Step-by-Step Plan

### Phase 1: 再生ボタンの最左配置 (UI Layout)
1. 下部コントローラーパネル（`fixed bottom-0` の div）内の Flex コンテナにおける要素の並び順を変更します。
2. 現在、中央に配置されている「再生/一時停止ボタン」を、最も左側（`playbackSpeed` ボタンの隣、または最左）に移動させます。
3. 全体のバランスを調整し、親指での操作性を最適化します。

### Phase 2: Grab to Scroll 機能の実装 (Interactive Logic)
1. **状態管理の追加**:
    - `isDragging` (boolean): 現在ドラッグ中かどうかを管理。
    - `startX` / `startY` (number): ドラッグ開始時の座標を記録。
    - `scrollLeft` / `scrollTop` (number): ドラッグ開始時の要素のスクロール位置を記録。
2. **イベントハンドラの作成**:
    - `onMouseDown` / `onTouchStart`: ドラッグ開始。カーソルを `grab` に変更し、現在の位置を記録。
    - `onMouseMove` / `touchMove`: ドラッグ中。移動量を計算し、要素の `scrollTop` を更新。カーソルを `grabbing` に変更。
    - `onMouseUp` / `touchEnd`: ドラッグ終了。状態をリセット。
3. **スタイルの適用**:
    - テキストエリアに `cursor: grab` を適用し、ドラッグ中は `cursor: grabbing` に切り替わるようにします。
    - `user-select: none` をドラッグ中に適用し、テキスト選択による干渉を防ぎます。

## 4. Analysis
- **リスク**: テキストの選択（コピー＆ペースト）機能との競合。
- **対策**: ドラッグの判定しきい値を設けるか、あるいはドラッグ操作中の間のみ `user-select: none` を動的に適用します。
- **代替案**: 標準的な DOM API を使用した軽量な自前実装を採用し、依存関係を増やさず「少しずつ」の改善を優先します。

## 5. Test Impact
- **検証項目**:
    - [ ] 再生ボタンが最左端に配置され、機能しているか。
    - [ ] PCマウスでテキストエリアを掴んで上下にスクロールできるか。
    - [ ] スマートフォン（タッチ操作）で同様に掴んでスクロールできるか。
    - [ ] 通常のテキスト選択機能が阻害されていないか。
    - [ ] スクロール中に意図しないページ全体のスクロール（バウンス）が発生しないか。
