# 音声再生デッドロック及び物理的ノイズ問題 調査・対応記録

## 1. 現象
- **症状**: 再生ボタン押下後、音声が出ず1行目で停止する（デッドロック）。
- **深刻な不具合**: 再生時にPCが振動するほどの激しい低音ノイズが発生し、ハードウェア（スピーカー）への物理的リスクが確認された。
- **挙動**: タブにスピーカーアイコンが表示されており、ブラウザがオーディオ出力を継続している。

## 2. 原因分析
### 2.1 音声デッドロックの原因
- `window.speechSynthesis` のキューが滞留し、`onstart` イベントが発火しない状態に陥っていた。
- Web Audio API (Ambient Sound) との同時起動によるリソース競合が疑われた。

### 2.2 物理的ノイズの原因
- `AudioController.speak` 以外に、`generateGuide` 等の関数内に「オーディオアンロック」用のダミー音声生成 (`SpeechSynthesisUtterance`) および一時的な `AudioContext` 生成コードが散在していた。
- これらのコードが意図せず暴走し、制御不能な低周波信号を出力し続けた。

## 3. 対処経緯
### Phase 1: APIエラーハンドリングの追加
- API 500エラーによる JSON パース失敗 $\rightarrow$ UIクラッシュ の連鎖を修正。

### Phase 2: 音声ロジックの刷新（AntiGravityによる試行）
- `cancel()` $\rightarrow$ `resume()` の強制覚醒シーケンスを導入。
- 環境音の開始タイミングを `onstart` 以降に遅延させる最適化を実施。
- しかし、依然としてデッドロックと物理的ノイズが発生。

### Phase 3: 完全モック化による切り分け（現在のフェーズ）
- **戦略**: すべての実音声出力を排除し、遷移ロジックのみを検証する。
- **実施内容**: 
  - `AudioController.speak` $\rightarrow$ `console.log` と `setTimeout` による擬似再現に置換。
  - `startAmbientSound` / `stopAmbientSound` $\rightarrow$ 中身を完全に空に。
  - **徹底排除**: `generateGuide` 等に潜んでいた「アンロック用ダミー音声」および `AudioContext` 生成コードをすべて削除。

## 4. 検証方針
1. **安全性の確認**: 物理的なノイズが完全に消えていること。
2. **ロジックの確認**: 音が出ない状態で、ハイライトが 1行目 $\rightarrow$ 2行目... と自動遷移するかを確認。
3. **結論の導出**: 遷移が正常なら「API競合」が原因、遷移しないなら「状態管理ロジック」が原因と断定する。
